@model List<GsFashion.Repository.Models.MenuTreeModel>

<ul class="nav flex-column sidebar-menu">
    @foreach (var node in Model)
    {
        @await Html.PartialAsync("_MenuNode", node)
    }
</ul>
